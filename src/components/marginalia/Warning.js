import React from "react";
import { Backdrop, CircularProgress } from "@material-ui/core";

export default function Warning() {
    return (
        // <Backdrop open={true} >
        //     <CircularProgress color="inherit" />
        // </ Backdrop>
        <div>error 404 reload page</div>
    )
}